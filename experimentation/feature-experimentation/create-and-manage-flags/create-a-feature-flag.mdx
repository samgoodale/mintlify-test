---
title: "Create a feature flag"
description: "This guide walks through creating a new feature flag."
---


## Access the Feature flag creation page

<Steps>
<Step>
Click **Activate** > **Feature flags** > **New feature flag**.
</Step>
<Step>
Name the feature flag and choose the project on which to activate the flag.
</Step>
<Step>
Associate a tag and a description (optional)
</Step>
<Step>
Click **Validate**.
</Step>
</Steps>

A feature key is automatically generated based on the feature flag's name; however, the feature key can be edited.

<Note>
**Note:**

The feature key is the unique ID that identifies the feature flag in source code. Update the feature key in Kameleoon before editing the feature key in source code.
</Note>

<Frame>
![feature-key](https://help.kameleoon.com/assets/images/feature-key-6612ae67db43093c2da02927ba9e377a.png)
</Frame>

By default, the flag is **OFF**. Activate the flag once the flag is configured.

## Structure of the Rollout Planner

<Frame>
![rollout-planner](https://help.kameleoon.com/assets/images/rollout-planner-0c46531f4a5b59e481a26da18be2aa38.png)
</Frame>

The Rollout planner consists of several blocks.

- A **header** indicating the flag's name, the associated sitecode (copy and paste the sitecode directly from the header), and the feature key. Click the flag's name to edit the flag's details.
- A **Setup** menu, to create and manage feature variables and feature variations URL.
- A **Rollout Planner** specific to each environment. Changes made to a feature flag are specific to the environment selected in the Rollout Planner section. Kameleoon enables linking production and staging/QA environments to the app.
- A **toggle** that turns the feature flag **On** or **Off**.
- An area to add rollout rules.
- A **toolbar** that provides access to the AI assistant, revert changes, and secondary actions (**Simulate** and **Calculate duration**).
- A **Save** button.

<Note>
**Note:**

Check that the flag has been created and that you can retrieve it from your source code by using our SDK (by calling the `isFeatureActive()` or `getVariation()` method). Then, navigate to your feature flag in Kameleoon and continue configuration.
</Note>