import { Select, Text, Bold, Italic, Code, CodeRef, Ref, If } from '../../utils.mdx';
import { Shared } from '../../shared.mdx';

<Select name={props.sec}>
<div name="tip_get_variations">

:::tip
If you want to iterate over all feature flags and call <CodeRef x={props.c.GetVariation}/> on each, use the <CodeRef x={props.c.GetVariations}/> method instead.
:::

</div>

<div name="description">

Returns a list of feature flag keys currently available for the SDK.
</div>

<div name="return_value">

| Type | Description |
|------|-------------|
| <Code x={props.c.GetFeatureList.Return}/> | List of feature flag keys |

</div>
</Select>
